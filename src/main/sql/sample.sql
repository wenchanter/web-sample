create table sample
(
   id                   int(10) not null,
   content              varchar(200) not null,
   userid               varchar(100) not null,
   time                 datetime not null,
   primary key(id)
);